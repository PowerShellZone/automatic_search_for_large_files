Get-ChildItem -Recurse -File |
Sort-Object -Property Length -Descending |
Select-Object -First 5 @{Name = "Lokalizacja"; Expression = { $_.FullName }}, @{Name = "Rozmiar [GB]"; Expression = { "{0:N2}" -f ($_.length / 1GB) }
}
